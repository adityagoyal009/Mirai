// Stub — slack extension not included in Mirai fork
export type ResolvedSlackAccount = { platform: "slack"; id: string };
export function resolveSlackAccount(): ResolvedSlackAccount | null {
  return null;
}
