// Stub — signal extension not included in Mirai fork
export type ResolvedSignalAccount = { platform: "signal"; id: string };
export function resolveSignalAccount(): ResolvedSignalAccount | null {
  return null;
}
