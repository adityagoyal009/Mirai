// Stub — telegram extension not included in Mirai fork
export type ResolvedTelegramAccount = { platform: "telegram"; id: string };
export function resolveTelegramAccount(): ResolvedTelegramAccount | null {
  return null;
}
