// Stub — discord extension not included in Mirai fork
export type ResolvedDiscordAccount = { platform: "discord"; id: string };
export function resolveDiscordAccount(): ResolvedDiscordAccount | null {
  return null;
}
