# 2026-03-28

- Rebuilt `website/index.html` into a premium static landing page for Mirai with stronger product positioning, a static brief-builder CTA, and pointer-reactive 3D motion on the hero and major cards.
- Added a public concierge intake flow for Mirai under `/landing`, `/signin`, and `/admin` without changing the private dashboard routes. Landing page now requires Google sign-in before submission, stores report requests in the portal DB, and includes a separate admin analytics board for request tracking.
